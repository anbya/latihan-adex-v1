import React, { Component } from 'react';
import {Button} from 'react-bootstrap'

class button extends Component {
    render() {
        return (
            <div>
                <Button variant='danger'>Awas Ini ujian </Button>
            </div>
        );
    }
}

export default button;